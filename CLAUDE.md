# voice-travel-assistant

دستیار سفر صوتی فارسی‌زبان که به جستجوی پرواز داخلی و خارجی، قطار، اتوبوس، هتل و تور پلتفرم ۷۸۰ وصل می‌شود.

## تصمیمات محصولی (از PRD)

**منبع:** `docs/PRD of AI Voice Travel Assistant Search Copilot.pdf` — سند رسمی محصول، مرجع تصمیمات محصولی.

- **تجربه‌ی مکالمه‌ای چندمرحله‌ای (Conversational Commerce):** وقتی یک فیلد الزامی کم باشه (مثلاً مبدا یا تاریخ)، سیستم باید با یک لحن گرم و طبیعی فارسی از کاربر بپرسه (clarification)، نه این‌که یک پیام خطای خشک برگردونه. فیلد `Missing` در خروجی `nlu.Parse` دقیقاً برای همین طراحی شده — مرحله‌ی بعد، تولید متن پرسش طبیعی از روی این فیلده.
- **فیلدهای الزامی برای پرواز داخلی:** فقط مبدا، مقصد، تاریخ. تعداد نفرات (`adults`) فقط برای هتل و پرواز خارجی اهمیت داره؛ در MVP فعلی (فقط پرواز داخلی) همیشه `adults=1` پیش‌فرضه و اصلاً از کاربر پرسیده نمی‌شه.
- **تمرکز MVP:** در ابتدا فقط پرواز داخلی (اولویت اول) و هتل (اولویت دوم) در محدوده بود. از آن زمان **قطار، اتوبوس، پرواز خارجی و تور هم پیاده‌سازی شدند** (بخش «وضعیت فعلی» را ببینید). تنها چیزی که خارج از محدوده مانده اقامتگاه/ویلا/بوم‌گردی است — و آن هم نه از روی اولویت‌بندی ما: ۷۸۰ اصلاً چنین سرویس مستقلی ندارد (اقامتگاه فقط به‌عنوان جزئی از پکیج تور وجود دارد، `tour/v2/accommodations`)، پس چیزی برای deep link کردن نیست. فقط برای آنالیتیکس تشخیص داده می‌شود تا بدانیم تقاضا چقدر است.
- **نمایش نتایج — معماری Redirect (موقت):** هدف نهایی طبق PRD (بخش ۷.۵) نمایش نتایج به‌صورت Carousel داخل چت با لینک مستقیم به ۷۸۰ است. چون این کار به یک توکن API از پلتفرم ۷۸۰ وابسته‌ست که هنوز نداریم، در MVP فعلی به‌جاش کاربر مستقیم به `searchUrl` سایت ۷۸۰ (همون چیزی که `internal/searchurl` می‌سازه) هدایت (redirect) می‌شه. وقتی توکن API در دسترس قرار گرفت، این redirect باید با Carousel واقعی جایگزین بشه.
- **مرتب‌سازی پیش‌فرض:** ارزان‌ترین (`sort=lowPrice`) — چون هر دو پرسونای هدف در PRD به قیمت حساس‌ان.
- **Edge caseهای مستندشده برای فازهای بعد (فعلاً فقط مستندسازی، بدون پیاده‌سازی):**
  - عدم وجود نتیجه → پیام «ظرفیت تکمیل» + پیشنهاد تغییر تاریخ یا ارجاع به پشتیبانی.
  - تغییر شدید قیمت (نسبت به انتظار کاربر) → نمایش هشدار.
  - لحن نامفهوم/گیج‌کننده‌ی کاربر → هدایت مستقیم به پشتیبانی انسانی.
- **Refinement (مثلاً «ارزون‌ترش رو بده»):** یکی از تعامل‌های کلیدی PRD است، ولی چون به نمایش نتایج *داخل* اپلیکیشن وابسته‌ست (نه صرفاً یک redirect)، پیاده‌سازیش منوط به دریافت توکن API و ساخت واقعی Carousel می‌مونه — قبلش معنا نداره.

## معماری

پروژه از سه بخش مستقل تشکیل شده:

- **backend/** — سرویس Go (Fiber). مسئول API، ماژول NLU برای تشخیص intent فارسی، و هماهنگی بین فرانت‌اند و automation-service.
- **automation-service/** — میکروسرویس Node.js با Playwright. مسئول اتوماسیون واقعی (جستجوی پرواز/هتل روی پلتفرم ۷۸۰). بک‌اند Go با این سرویس از طریق HTTP داخلی ارتباط برقرار می‌کند.
- **frontend/** — اپلیکیشن React با پشتیبانی کامل RTL فارسی و فونت Vazirmatn. ورودی صدا از طریق Web Speech API مستقیماً سمت کلاینت انجام می‌شود (بدون نیاز به سرویس STT جداگانه).

## چرا این تصمیمات فنی

- **Playwright در سرویس جدا (نه در Go):** Playwright کتابخانه رسمی Go ندارد. به همین دلیل تمام منطق اتوماسیون مرورگر در یک میکروسرویس Node.js مجزا (`automation-service`) پیاده‌سازی شده و بک‌اند Go فقط از طریق HTTP آن را صدا می‌زند. این جداسازی باعث می‌شود بک‌اند اصلی سبک و مستقل از وابستگی‌های سنگین Playwright/Chromium بماند.
- **Web Speech API سمت کلاینت:** تشخیص گفتار در مرورگر کاربر انجام می‌شود، نه در بک‌اند — نیازی به ارسال استریم صوتی به سرور نیست.
- **ماژول NLU در بک‌اند Go:** تشخیص intent فارسی (مثلاً "بلیط تهران به مشهد برای فردا") در `backend/internal/nlu` انجام می‌شود و نتیجه به‌صورت پارامترهای ساخت‌یافته به `automation-service` پاس داده می‌شود.

## ساختار پوشه‌ها

```
voice-travel-assistant/
├── backend/                  # Go (Fiber)
│   ├── cmd/server/main.go    # entry point
│   ├── internal/
│   │   ├── nlu/               # تشخیص intent فارسی
│   │   ├── automation/        # کلاینت HTTP به automation-service
│   │   ├── handlers/          # HTTP handlers (health, ...)
│   │   └── config/            # بارگذاری env/config
│   ├── .env.example
│   └── go.mod
├── automation-service/       # Node.js + Playwright
│   ├── src/
│   └── package.json
├── frontend/                  # React (RTL + Vazirmatn + Web Speech API)
│   ├── src/
│   └── package.json
├── deploy.sh                  # اسکریپت دیپلوی خودکار (نگاه کنید به بخش «دیپلوی»)
└── CLAUDE.md
```

## دیپلوی

سرور تولید: `vta-server` (VPS به IP `194.62.43.142`، Ubuntu 26.04)، با کاربر اپلیکیشن غیر-root به نام `vtaapp`.

**از این به بعد دیپلوی همیشه از طریق اسکریپت خودکار انجام می‌شود، نه به‌صورت دستی:**

```
ssh vta-server "su - vtaapp -c 'nohup /var/www/vta/deploy.sh main > /tmp/deploy-$(date +%s).log 2>&1 &'"
```

(آرگومان اول نام برنچ است؛ در نبود آرگومان پیش‌فرض `main` است.)

### استراتژی برنچ

- **`dev`** — برنچ کاری. توسعه و کامیت‌های روزمره این‌جا انجام می‌شود.
- **`main`** — برنچ رسمی دیپلوی. وقتی یک مرحله روی staging تأیید شد، `dev` به `main` merge می‌شود (ترجیحاً fast-forward، بدون کامیت merge اضافی) و دیپلوی از `main` انجام می‌گیرد.

قبل از merge حتماً تأیید کنید که fast-forward تمیز است:

```
git log --oneline dev..main    # باید خالی باشد
git merge-base --is-ancestor main dev && echo FAST_FORWARD_SAFE
```

اگر `dev..main` خالی نبود یعنی `main` کامیتی دارد که در `dev` نیست — متوقف شوید و اول بررسی کنید.

> **پیشینه (۲۰۲۶-۰۷-۲۸ حل شد):** تا این تاریخ `main` روی کامیت اول (`ec5bffc "Initial project scaffold"`) مانده بود و ۲۸ کامیت عقب بود، در حالی که پروداکشن از `dev` دیپلوی می‌شد. آن وضعیت یک مین بی‌صدا بود: اسکلت اولیه هم `GET /health` دارد و `200` برمی‌گرداند، پس `deploy.sh main` اسکلت را build می‌کرد، symlink را عوض می‌کرد، سرویس را ری‌استارت می‌کرد، **health check پاس می‌شد**، rollback فعال نمی‌شد و اسکریپت با کد خروج صفر و پیام «Deploy complete» تمام می‌شد — یعنی پروداکشن بی‌سروصدا به نسخه‌ی اول برمی‌گشت بدون هیچ خطایی در هیچ لاگی. حالا `main == dev` است و این خطر رفع شده. **درسِ ماندگار: یک health check که فقط `200` را چک کند، رگرسیون نسخه را تشخیص نمی‌دهد — باید چیزی را بسنجد که مخصوص همان نسخه است** (همان کاری که health check فرانت با نام bundle می‌کند، پایین را ببینید).

### ⚠️ دیپلوی را detach کنید

اسکریپت را با `nohup` و `&` اجرا کنید (مثل دستور بالا). `deploy.sh` با `set -e` اجرا می‌شود و اگر سشن SSH وسط کار قطع شود، پروسه کشته می‌شود و دیپلوی **در وسط** رها می‌ماند. یک نمونه‌ی واقعی (۲۰۲۶-۰۷-۲۷): دیپلوی بین `go build` و کپی `.env` مُرد و یک release ساخته‌شده ولی وصل‌نشده باقی گذاشت، در حالی که `current` هنوز به release قبلی اشاره می‌کرد و هیچ خطایی هم جایی ثبت نشده بود.

`deploy.sh` (که نسخه‌ی مرجعش همین‌جا در ریشه‌ی ریپو نگه‌داری می‌شود و با `cat` مستقیم روی سرور در مسیر `/var/www/vta/deploy.sh` هم قرار دارد) این مراحل را خودکار انجام می‌دهد:

1. یک release جدید با نام `v-<تاریخ>-<ساعت>` در `/var/www/vta/releases/` می‌سازد و ریپو را (با retry در برابر قطعی‌های شبکه‌ای گیت‌هاب) در آن clone می‌کند.
2. بک‌اند Go را build می‌کند و `.env` واقعی را از `/var/www/vta/shared/.env` (که بین release‌ها مشترک و خارج از گیت است) در release کپی می‌کند.
3. فرانت‌اند را در همان release می‌سازد (`npm ci` سپس `npm run build` با `VITE_API_BASE=""`)، وجود `dist/index.html` را چک می‌کند، و `node_modules` فرانت را (که فقط ابزار build است، ~۴۰ مگابایت) پاک می‌کند.
4. اگر `automation-service` دیپندنسی واقعی داشته باشد، `npm install --production` می‌زند.
5. symlink مسیر `/var/www/vta/current` را به‌صورت atomic به release جدید تغییر می‌دهد و سرویس‌های systemd (`vta-backend` و `vta-automation`) را ری‌استارت می‌کند. چون ریشه‌ی Nginx هم زیر همین symlink است، فرانت در همان لحظه با بک‌اند عوض می‌شود.
6. سه health check می‌زند: بک‌اند (`GET /health`)، automation-service (پورت ۴۰۰۰)، و فرانت (که `index.html` سرو‌شده باید به bundle هش‌دار همین release اشاره کند). در صورت شکست هرکدام، به‌طور خودکار symlink و سرویس‌ها را به release قبلی برمی‌گرداند (rollback) و با کد خروج غیرصفر خارج می‌شود.
7. در صورت موفقیت، فقط ۳ release آخر را نگه می‌دارد و بقیه را پاک می‌کند.

**نکته:** خود `deploy.sh` روی سرور در `/var/www/vta/deploy.sh` است، یعنی **بیرون** از `releases/`. پس اسکریپت خودش را به‌روز نمی‌کند — بعد از تغییر دادنش در ریپو، باید نسخه‌ی سرور را دستی همگام کنید (`scp deploy.sh vta-server:/var/www/vta/deploy.sh`).

### فرانت‌اند هم توسط `deploy.sh` دیپلوی می‌شود — دستی لازم نیست

`deploy.sh` فرانت‌اند را داخل خود release می‌سازد (`npm ci` سپس `npm run build`)، و **ریشه‌ی Nginx روی `/var/www/vta/current/frontend/dist` تنظیم است**. یعنی همان symlink اتمیکی که بک‌اند را جابه‌جا می‌کند، فرانت را هم با خودش می‌برد. نتیجه: فرانت و بک‌اند ساختاراً نمی‌توانند از هم جدا بیفتند، و rollback هر دو را با هم برمی‌گرداند. **هیچ کپی دستی‌ای لازم نیست.**

دو نکته‌ی ظریف در پیاده‌سازی:

- **`VITE_API_BASE=""` عمداً رشته‌ی خالی است، نه unset.** در `api.js` این مقدار با `??` خوانده می‌شود، پس `""` یعنی same-origin (که Nginx مسیر `/api/` را proxy می‌کند)، در حالی که unset بودن به `http://localhost:8080` برمی‌گردد و در مرورگر هر کاربری جز خود سرور می‌شکند.
- **health check فرانت، نام bundle هش‌دار همان release را در `index.html` سرو‌شده چک می‌کند** — نه صرفاً کد وضعیت `200`. دلیلش دقیقاً همان درس بالاست: فرانت کهنه هم تمام مدت `200` برمی‌گرداند.

> **پیشینه (۲۰۲۶-۰۷-۲۸ حل شد):** قبلاً `deploy.sh` فقط بک‌اند Go را build می‌کرد و Nginx فرانت را از `/var/www/vta/frontend` سرو می‌کرد — یک دایرکتوری مستقل (مالکش `www-data`) که هیچ دیپلویی به آن دست نمی‌زد. برای همین هر دیپلوی بی‌صدا فرانت را روی هر نسخه‌ای که آخرین بار دستی کپی شده بود باقی می‌گذاشت. عارضه‌ی واقعی‌اش در انتشار آنالیتیکس دیده شد: بک‌اند `parses` را ثبت می‌کرد ولی فرانتِ سرو‌شده مال روز قبل بود و اصلاً `X-Session-Id` و رویدادهای `/api/events` را نمی‌فرستاد، پس `session_id` همیشه خالی بود و جدول `events` برای همیشه خالی می‌ماند.

### شبکه: پورت ۸۰ و ۴۴۳ از ایران فیلترند

**این مشکل سرور نیست — دنبال باگ در کانفیگ سرور نگردید.** روی خود سرور همه‌چیز سالم است: Nginx روی `0.0.0.0:80` گوش می‌دهد، UFW پورت‌های ۸۰/۴۴۳/۸۴۴۳ را باز کرده، و `curl http://localhost/health` روی سرور جواب `{"status":"ok"}` می‌دهد. ولی از مسیر شبکه‌ی ایران، پورت ۸۰ تایم‌اوت می‌خورد در حالی که پورت‌های ۲۲ و ۸۴۴۳ از همان‌جا باز هستند و `https://stage.780assistant.ir:8443/health` جواب می‌دهد. یعنی فیلترینگ روی خودِ پورت است، نه قطعیِ کلیِ شبکه یا مشکل VPS.

(دقت: پورت ۴۴۳ هم از بیرون تایم‌اوت می‌خورد، ولی این به‌تنهایی اثبات فیلترینگ نیست چون در حال حاضر هیچ سرویسی روی ۴۴۳ گوش نمی‌دهد — تنها listenerهای سرور `:80` و `:8080` و `:8443` هستند.)

**به همین دلیل staging روی پورت غیراستاندارد ۸۴۴۳ سرو می‌شود:**

- آدرس: `https://stage.780assistant.ir:8443`
- کانفیگ: `/etc/nginx/sites-enabled/vta-ssl` (پورت ۸۰ در `sites-enabled/vta` جداگانه باقی مانده و از داخل سرور کار می‌کند).
- گواهی: Let's Encrypt برای دامنه‌ی `stage.780assistant.ir`.

**⏰ یادآوری تمدید دستی گواهی — انقضا: ۲۰۲۶-۱۰-۲۱.** تمدید خودکار certbot به چالش HTTP-01 روی پورت ۸۰ متکی است که از بیرون در دسترس نیست، پس روی تمدید خودکار حساب نکنید. قبل از این تاریخ گواهی را دستی تمدید کنید (مثلاً با چالش DNS-01)، وگرنه staging با خطای TLS از کار می‌افتد.

جزئیات باقی‌مانده‌ی زیرساخت سرور (Go، Node، Docker، systemd unitهای `vta-backend` و `vta-automation` در `/etc/systemd/system/`) در این فایل مستند نیست و باید در کانفیگ خود سرور بررسی شود.

## ماژول NLU (`backend/internal/nlu`)

قاعده‌محور (rule-based) است، بدون LLM — متن فارسی حاصل از Web Speech API را می‌گیرد و intent/مبدا/مقصد/تاریخ را استخراج می‌کند. به `POST /api/parse` وصل است.

**⚠️ ریسک شناخته‌شده — قاعده‌ی fallback تشخیص intent:**
اگر هیچ کلیدواژه‌ی صریحِ سرویس در جمله نباشد (قطار، اتوبوس، پرواز، هتل — که در `intent.go` هرکدام لیست کلیدواژه‌ی خودشان را دارند و intent با **زودترین** کلیدواژه در جمله انتخاب می‌شود) ولی نام یک شهر شناخته‌شده در جمله باشد، ماژول فرض می‌کند intent، `flight_search` است. این یعنی جمله‌ای مثل **«تهران هوا چطوره»** (که هیچ ربطی به سفر ندارد) هم به‌عنوان جستجوی پرواز تشخیص داده می‌شود.
- **چرا فعلاً قابل قبوله:** در MVP فعلی، کاربر می‌داند دارد با یک دستیار سفر صحبت می‌کند، پس هر جمله‌ی حاوی نام شهر تقریباً همیشه واقعاً مرتبط با سفر است.
- **چه زمانی باید بازنگری بشه:** اگر دامنه‌ی محصول گسترده‌تر شد (مثلاً این دستیار به سؤالات عمومی‌تر هم جواب داد، نه فقط سفر)، این fallback باید محدودتر یا حذف بشه، چون نرخ false-positive بالا می‌ره.

**تشخیص مبدا/مقصد (`extractOriginDestination` در `nlu.go`) — منطق سلسله‌مراتبی:**
برای intentهای مسیری (پرواز، قطار، اتوبوس)، به این ترتیب اولویت اعمال می‌شه:
1. اگه کلمه‌ی مستقل «از» با یک شهر شناخته‌شده در فاصله‌ی کم (حداکثر ۳ کلمه، ثابت `maxAzWordGap`) بعدش اومده باشه → اون شهر = مبدا. اگه «به» هم بود، شهر بعد از «به» = مقصد؛ وگرنه هر شهر دیگه‌ای که جای دیگه‌ی جمله باشه (چه قبل چه بعد از «از») = مقصد. این حالت‌های معکوس مثل «می‌خوام برم مشهد از تهران» رو هم درست پوشش می‌ده.
2. اگه «از» نبود ولی «به» بود → شهر قبل از «به» (اگه بود) = مبدا، شهر بعدش = مقصد.
3. اگه هیچ‌کدوم نبود → ۱ شهر یعنی فقط مقصد (مبدا نامشخص)؛ ۲+ شهر یعنی یک route کوتاه («تهران مشهد») و اولی=مبدا، دومی=مقصد.

برای intent هتل، این منطق اصلاً اجرا نمی‌شه — همیشه فقط اولین شهر پیدا‌شده = مقصد، مبدا همیشه `nil` (چون «هتل مشهد تهران» به‌معنای مسیر نیست).

⚠️ **نکته‌ی فنی مهم برای هر قاعده‌ی جدید متن‌محور:** موقع پیاده‌سازی این منطق، دو باگ substring پیدا شد که هر دو از یک الگو بودن — چند تا کلمه‌ی فارسی، کلمه‌ی کوتاه‌تر دیگه‌ای رو به‌عنوان substring دربر دارن:
- «شنبه» زیررشته‌ی «یکشنبه»، «دوشنبه»، ... است (برای تشخیص روز هفته در `dates.go`).
- «به» زیررشته‌ی انتهای همون اسم‌های روز هفته است (چون «شنبه» به «ب+ه» ختم می‌شه) — یعنی `strings.Index(text, "به")` ساده داخل «پنجشنبه» هم match می‌کرد!

هر دو با تبدیل به تطبیق «کلمه‌ی مستقل» (split بر اساس space و مقایسه‌ی دقیق، نه substring) حل شدن. **قانون کلی برای آینده: هر keyword/marker جدیدی که به این ماژول اضافه می‌شه، باید با تطبیق کلمه‌ی مستقل چک بشه، نه `strings.Contains`/`strings.Index` ساده** — وگرنه احتمال match اشتباه داخل یک کلمه‌ی دیگه هست.

## یکپارچه‌سازی با ۷۸۰ — deep link (پرواز کار می‌کند، هتل نه)

معماری محصول ما «Redirect» است: از روی slotها یک URL نتایج ۷۸۰ می‌سازیم و کاربر را به آن می‌فرستیم. این برای **پرواز** کاملاً کار می‌کند، ولی برای **هتل** — پس از تست در سشن تمیز (Incognito) — مشخص شد که کار نمی‌کند.

**یافته‌های قطعی (تست‌شده در سشن تمیز، بدون آلودگی کش):**
- **صفحه‌ی نتایج هتل `/tourism/hotel/search/{city}?...` پارامترهای query را نمی‌خواند.** حتی یک URL واقعیِ کپی‌شده از یک جستجوی موفق، در سشن تازه مقصد را `undefined` می‌گیرد و تاریخ‌ها به پیش‌فرض برمی‌گردند. سه حالت تست شد و **هر سه شکست خوردند**: با `requestId`، بدون `requestId`، و با `readCache=false`. صفحه احتمالاً وابسته به state داخلی برنامه (SPA) است که فقط با ناوبری درون‌اپ پر می‌شود، نه از URL.
  - ⚠️ **درس مهم:** تست‌های اولیه‌ی ما که «موفق» به‌نظر می‌رسیدند، **آلوده به کش سشن مرورگر** بودند. هر تست یکپارچه‌سازی با ۷۸۰ باید در سشن تمیز/Incognito انجام شود.
- **صفحه‌ی لندینگ `/tourism/hotel/{cityNameEngLower}` شهر را درست تشخیص می‌دهد** و هتل‌های همان شهر را نشان می‌دهد، **ولی تاریخ را از query نمی‌پذیرد** (`checkInDate`/`checkOutDate` بی‌اثرند).
- **پرواز سالم است:** `/tourism/flights/THR-MHD?...&departureDate=...` در سشن تمیز درست کار می‌کند.

**راه‌حل واقعی:** گرفتن فرمت deep link رسمی هتل از تیم ۷۸۰ (در صف است، مثل توکن API). تا آن زمان، منطق nights/تاریخ در NLU و فرانت **دست‌نخورده** نگه داشته می‌شود؛ فقط در ساخت URL استفاده نمی‌شود.

**راه‌حل موقت فعلی (تا رسیدن deep link رسمی):** به‌جای URL صفحه‌ی نتاییجِ خراب، کاربر را به **صفحه‌ی لندینگ** `https://780.ir/tourism/hotel/{cityNameEngLower}` می‌فرستیم (که حداقل شهر درست است) و در کارت تأیید هتل یک پیام می‌گذاریم که تاریخ (که از slots پر می‌شود) باید روی خود سایت ۷۸۰ انتخاب شود. کد این کار در `frontend/src/hotelCities.js` (`buildHotelSearchUrl`) با کامنت واضحِ «TEMPORARY» علامت‌گذاری شده. دلیل: فرستادن کاربر به صفحه‌ی نتایجی که مقصدش `undefined` است، از نداشتن هتل هم بدتر است.

## وضعیت فعلی

اپلیکیشن به‌صورت انتها-به-انتها کار می‌کند و روی staging در دسترس است: **`https://stage.780assistant.ir:8443`** (بخش «دیپلوی» را برای دلیل پورت ۸۴۴۳ ببینید).

**شش سرویس پشتیبانی می‌شود** (intentها در `internal/nlu/nlu.go`): `flight_search` (داخلی)، `international_flight_search`، `train_search`، `bus_search`، `hotel_search`، `tour_search` — به‌علاوه‌ی `help` و `unknown`.

**تور — شکل متفاوت slot:** تور نه مبدا می‌گیرد نه تاریخ (۷۸۰ نتایج را با فیلتر *ماه* محدود می‌کند، نه روز)، پس «تور کیش» با یک کلمه کامل است. برای همین در فرانت فیلد `requires` به `SERVICES` اضافه شد و `computeMissing` روی همان حلقه می‌زند، به‌جای شرط‌های پراکنده‌ی per-intent که داشت انباشته می‌شد.

**⚠️ قانون سفت‌وسخت تور — هرگز slug حدس نزنید.** slug نامعتبر در ۷۸۰ خطای ۴۰۴ نمی‌دهد؛ صفحه در حالت لودینگ **بی‌نهایت** گیر می‌کند بدون هیچ پیامی — یعنی بدتر از صفحه‌ی خالی هتل. هر مقصدی که در جدول نیست باید پیام «پشتیبانی نمی‌کنیم» بگیرد. مقاصد از endpointهای `tour/v2/zones/{DOMESTIC,INTERNATIONAL}` آمده‌اند و چون آن endpointها احراز هویت می‌خواهند و تور هیچ sitemap ندارد، **این تنها واژگانی است که از روی چیزی عمومی قابل بازتولید نیست** — به همین دلیل خودِ JSON خام در `docs/tour-zones-*.json` کامیت شده.

**پرواز خارجی — نکته‌ی معماری:** این intent مستقیماً تشخیص داده **نمی‌شود**. `detectIntent` اول طبق همان اولویت کلیدواژه‌ی قبلی به `flight_search` می‌رسد (پس «بلیط قطار …» هنوز قطار است)، و بعد `parse` وقتی شهرها معلوم شدند آن را ارتقا می‌دهد. دلیلش این است که تصمیم داخلی/خارجی به **شهر** وابسته است نه به لفظ: «بلیط تهران به استانبول» باید خارجی شود بدون این‌که کلمه‌ی «خارجی» بیاید. کلیدواژه‌ی صریح «خارجی»/«بین المللی» هم آن را اجبار می‌کند تا کاربر بتواند قبل از گفتن شهر درخواست بدهد.

**پوشش شهرها** — واژگان مشترک در `internal/nlu/cities.go` شامل **۷۳ شهر** است (با aliasها، مثلاً «تهرون»). این‌که هر شهر واقعاً با کدام سرویس قابل جستجوست، با جدول‌های جدا‌گانه‌ی هر سرویس در `internal/searchurl` تعیین می‌شود (کلید: نام فارسی شهر، نه کد IATA):

| سرویس | تعداد شهر | منبع |
|---|---|---|
| پرواز داخلی | ۵۵ (شهرهایی که `IATA` غیرخالی دارند) | `cities.go` |
| پرواز خارجی | ۳۳۵ شهر خارجی (+ شهرهای ایرانی به‌عنوان مبدا) | `intlcities.go` |
| قطار | ۳۶ | `transitcities.go` (`trainCities`) |
| اتوبوس | ۶۳ | `transitcities.go` (`busCities`) |
| هتل | ۸ | `hotelcities.go` (`hotelCities`) |
| تور | ۱۸ (۱۰ داخلی + ۸ خارجی) | `tourdestinations.go` |

۱۸ شهر عمداً `IATA` خالی دارند (فرودگاه ندارند و فقط با قطار/اتوبوس قابل دسترسی‌اند، مثل قم و بابل).

**بک‌اند (Go / Fiber)** — روتها در `internal/handlers/health.go`:

- `GET /health` → `{"status":"ok"}`
- `GET /api/test-automation/flights` — endpoint تست automation-service.
- `POST /api/parse` — متن فارسی می‌گیرد، JSON شامل `intent`/`origin`/`destination`/`date`/`nights`/`adults`/`missing`/`searchUrl`/`citySupported` برمی‌گرداند. ساخت URL در `internal/searchurl` است (جدا از `nlu`) و برای هر شش سرویس تابع جدا دارد. برای پرواز خارجی `returnDate` هم برمی‌گردد (فقط در رفت‌وبرگشت).
- `POST /api/events` — دریافت رویدادهای قیف از فرانت‌اند برای آنالیتیکس.
- CORS فقط `http://localhost:5173` را می‌پذیرد (نه wildcard)، با هدرهای مجاز `Content-Type`, `ngrok-skip-browser-warning`, `X-Session-Id`. در پروداکشن فرانت same-origin سرو می‌شود، پس CORS اصلاً درگیر نمی‌شود.

**تشخیص سرویس‌های پشتیبانی‌نشده** (`internal/nlu/unsupported.go`) — اگر کاربر چیزی بخواهد که نداریم (`tour`، `villa`/بوم‌گردی، `international` یعنی پرواز خارجی)، برچسبش **فقط برای آنالیتیکس** ثبت می‌شود و عمداً روی `detectIntent` یا پاسخ اثری ندارد (تست رگرسیون دارد) تا هرگز یک درخواست معتبر را ندزدد.

**فرانت‌اند (React + Vite، RTL، Vazirmatn)** — دیگر هارنس تست نیست؛ یک UI مکالمه‌ای کامل در `src/App.jsx` است:

- **ورودی صوتی** با Web Speech API (`useSpeechRecognition.js`, `lang: 'fa-IR'`)، با fallback به ورودی متنی و مدیریت خطا برای کدهای خام Web Speech (شامل مسیر مخصوص iOS).
- **خروجی صوتی فارسی** (TTS) برای پیام‌های سیستم (`useSpeechSynthesis.js`)، با تشخیص نبودِ صدای فارسی و افت آبرومندانه.
- **حلقه‌ی clarification** — slotها سمت فرانت انباشته می‌شوند و برای هر فیلد `missing` یک پرسش طبیعی فارسی نمایش داده می‌شود (این همان قدمی است که در بخش «تصمیمات محصولی» طراحی شده بود و حالا پیاده شده)، به‌علاوه‌ی یک کارت تأیید قبل از هدایت به ۷۸۰.
- **آنالیتیکس** — `src/session.js` یک UUID ناشناس در `sessionStorage` می‌سازد که در هدر `X-Session-Id` می‌رود، و `sendEvent` در `src/api.js` رویدادهای قیف را fire-and-forget می‌فرستد: `clarification_shown`, `clarification_answered`, `confirm_shown`, `search_clicked`, `correction_clicked`, `new_search`, `error_shown`, `help_shown`, `voice_error`.

**آنالیتیکس (`internal/analytics` + `docs/analytics.md`)** — ثبت ناشناس هر تعامل در SQLite (مسیر از env `VTA_ANALYTICS_DB`؛ اگر خالی باشد کاملاً خاموش است و اپ سالم کار می‌کند). دو جدول: `parses` (سمت سرور، خودکار) و `events` (از فرانت). جزئیات schema و کوئری‌های آماده در `docs/analytics.md`.

**automation-service** — دیپندنسی‌ها (`express`, `p-queue`, `playwright`) در `package.json` تعریف شده‌اند و سرویس systemd `vta-automation` روی پورت ۴۰۰۰ بالاست. ولی معماری فعلی محصول «Redirect» است، یعنی مسیر اصلی کاربر از `searchUrl` می‌گذرد و به Playwright وابسته نیست؛ اسکریپت‌های `discover-*` در `src/` ابزار اکتشاف دستی برای مهندسی معکوس صفحات ۷۸۰ هستند و جزو مسیر اجرای محصول نیستند.

**آنچه هنوز باقی است:** نمایش نتایج به‌صورت Carousel داخل چت و Refinement («ارزون‌ترش رو بده») — هر دو منتظر توکن API از ۷۸۰. deep link هتل هم هنوز رسمی نشده (بخش «یکپارچه‌سازی با ۷۸۰» را ببینید).
